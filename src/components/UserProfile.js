// src/pages/UserProfile.js
import React from 'react';
import styled from 'styled-components';

const ProfileContainer = styled.div`
  padding: 2rem;
`;

const UserProfile = () => {
  return (
    <ProfileContainer>
      <h2>User Profile</h2>
      {/* Add user profile information and functionality here */}
    </ProfileContainer>
  );
}

export default UserProfile;
